# Exp-6: Mini Project — Full-Fledged DApp Using Ethereum / Hyperledger

> **Status**: 🔴 Pending — complete the experiment fully before writing this file.
> Refer to `docs/EXP-X_DOC_RULE.md` for all authoring rules.
> Refer to `docs/BLOCKCHAIN_LAB_MANUAL.md` Mini Project Documentation section for the report structure.

---

## AIM

<!-- TODO: Write the aim of the specific DApp chosen for the Mini Project.
     Format: "To design and develop a full-fledged decentralised application (DApp)
     for <domain/use-case> using <primary tech stack>." -->
To design and develop a full-fledged decentralised application (DApp) using Ethereum / Hyperledger.

---

## THEORY

<!-- TODO: Write 250–300 words (bullet points only, no prose).
     Mandatory web search to verify current versions before writing.
     Cover: DApp architecture (frontend + smart contract + blockchain),
     Foundry (version, year, written in Rust, by Paradigm),
     Hardhat 3 (version, breaking changes from v2), Solidity (version),
     OpenZeppelin contracts used (version), Anvil local node,
     chosen DApp domain concept (e.g., DEX, NFT, voting, supply chain),
     relevant EIP/ERC standards used. -->

---

## IMPLEMENTATION

### CODE

<!-- TODO: Include 3–8 code snippets in logical execution order:
     1. Main contract (src/<ContractName>.sol — SPDX + pragma + key state + functions)
     2. Foundry test file (test/<ContractName>.t.sol — setUp + key test)
     3. Foundry deploy script (script/Deploy.s.sol — Script contract with run() function)
     4. Hardhat deploy script (scripts/deploy.js — ethers.js deploy flow)
     5. hardhat.config.js — networks block (anvil + sepolia with env vars)
     6. foundry.toml — remappings and compiler config
     Refer to docs/EXP-X_DOC_RULE.md §7.1 for snippet rules. -->

---

### OUTPUT

<!-- TODO: Add screenshots saved in Exp-6/screenshots/.
     Minimum required:
       Fig 6.1 — Terminal: forge build or npx hardhat compile (Compiler run successful)
       Fig 6.2 — Terminal: forge test -vvv (all tests PASS with function names visible)
       Fig 6.3 — Terminal: deploy script output (contract address + tx hash)
       Fig 6.4 — Browser/MetaMask: DApp frontend interface with wallet connected
     Optional but recommended:
       Fig 6.5 — Etherscan Sepolia: verified contract page
       Fig 6.6 — Terminal: forge coverage output (coverage % per file)
     Follow naming: fig-6.Y-description.png (kebab-case, no spaces).
     Refer to docs/EXP-X_DOC_RULE.md §7.2 for screenshot rules.
     ⚠️ Use Chrome browser / Chrome DevTools MCP for all browser screenshots.
     ⚠️ Redact private key if visible in any terminal output. -->

---

## LAB OUTCOMES

<!-- TODO: Copy verbatim from docs/BLOCKCHAIN_LAB_MANUAL.md -->
**LO6** — Develop and test a Full-fledged DApp using Ethereum/Hyperledger.

---

## CONCLUSION

<!-- TODO: Write exactly 2–3 sentences.
     Mandatory opener: "We have successfully..."
     Mention: DApp name/domain, Foundry, Hardhat 3, Solidity, Sepolia (if deployed), LO6 achieved.
     No bullet points. Refer to docs/EXP-X_DOC_RULE.md §9. -->

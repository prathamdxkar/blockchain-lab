# Exp-3: To Study Deployment and Publish Smart Contracts on Ethereum Test Network

> **Status**: 🔴 Pending — complete the experiment fully before writing this file.
> Refer to `docs/EXP-X_DOC_RULE.md` for all authoring rules.

---

## AIM

<!-- TODO: Copy verbatim from docs/BLOCKCHAIN_LAB_MANUAL.md -->
To study Deployment and publish smart contracts on Ethereum test network.

---

## THEORY

<!-- TODO: Write 250–300 words (bullet points only, no prose).
     Mandatory web search to verify current versions before writing.
     Cover: Ethereum test networks (Sepolia — current active testnet, year launched),
     Hardhat (version), Alchemy / Infura RPC providers, MetaMask (version),
     deployment lifecycle on testnet (compile → deploy → verify), Etherscan,
     gas and ETH faucets, deprecated testnets (Ropsten/Rinkeby/Goerli — do NOT recommend). -->

---

## IMPLEMENTATION

### CODE

<!-- TODO: Include 3–8 code snippets in logical execution order:
     1. Contract to deploy (Solidity — SPDX + pragma + key functions)
     2. Hardhat config — Sepolia network block with env vars
     3. Hardhat deploy script (scripts/deploy.js — ethers.js deploy flow)
     4. .env.example — showing required keys (PRIVATE_KEY, ALCHEMY_API_KEY, ETHERSCAN_API_KEY)
     5. Hardhat test — local test run before testnet deploy
     6. Etherscan verification command (npx hardhat verify ...) as shell snippet
     Refer to docs/EXP-X_DOC_RULE.md §7.1 for snippet rules. -->

---

### OUTPUT

<!-- TODO: Add screenshots saved in Exp-3/screenshots/.
     Minimum required:
       Fig 3.1 — Terminal: npx hardhat run scripts/deploy.js --network sepolia output (tx hash visible)
       Fig 3.2 — Etherscan Sepolia: transaction confirmation page (tx hash, block, contract address)
       Fig 3.3 — MetaMask: Sepolia network selected + balance
     Optional but recommended:
       Fig 3.4 — Etherscan: verified contract page (green checkmark badge)
     Follow naming: fig-3.Y-description.png (kebab-case, no spaces).
     Refer to docs/EXP-X_DOC_RULE.md §7.2 for screenshot rules.
     ⚠️ Redact private key if visible in any terminal output. -->

---

## LAB OUTCOMES

<!-- TODO: Copy verbatim from docs/BLOCKCHAIN_LAB_MANUAL.md -->
**LO3** — Write and deploy smart contract using Remix IDE and MetaMask.

---

## CONCLUSION

<!-- TODO: Write exactly 2–3 sentences.
     Mandatory opener: "We have successfully..."
     Mention: contract name, Hardhat, Sepolia testnet, Etherscan, LO3 achieved.
     No bullet points. Refer to docs/EXP-X_DOC_RULE.md §9. -->
